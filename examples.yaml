Homepage
Primary navigation
Project
E
easylearning-front-e2e

Pinned

Manage

Plan

AI

Code

Build

Secure

Deploy

Operate

Monitor

Analyze

Help

Collapse sidebar
ManagementDrives
developers
Easylearning
easylearning-front-e2e
Files
Search files (*.vue, *.rb...)
f
.gi
‎thub‎
work
‎flows‎
playwri
‎ght.yml‎
tests-e
‎xamples‎
demo-todo-
‎app.spec.js‎
te
‎sts‎
locale.
‎spec.js‎
login.
‎spec.js‎
recover
‎.spec.js‎
register
‎.spec.js‎
role.avatar.
‎menu.spec.js‎
search.hidecom
‎pleted.spec.js‎
.giti
‎gnore‎
READ
‎ME.md‎
package-
‎lock.json‎
packag
‎e.json‎
playwright
‎.config.js‎
easylearning-front-e2e
.github
workflows
playwright.yml
playwright.yml
maytorres1's avatar
added playwright
Most recent commit.
May Torres authored Jan 27, 2025
b693849b
 Code owners
Assign users and groups as approvers for specific file changes. Learn more.
playwright.yml
669 B
name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
        node-version: lts/*
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run Playwright tests
      run: npx playwright test
    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30